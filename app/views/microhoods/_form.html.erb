
<div class='form'>
  <%= form_for @microhood, html: {class: 'form'} do |f| %>

  <% if @microhood.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(@microhood.errors.count, 'error') %> prohibited this microhood from being saved:</h2>

      <ul>
        <% @microhood.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='input-group'>
      <%= f.label :neighborhood %>
      <%= f.select :neighborhood_id, options_for_select(@neighborhoods_collection), {}, { class: 'input-group-field' } %>
  </div>
  <div class='input-group'>
    <%= f.label :name %>
    <%= f.text_field :name, class: 'input-group-field' %>
  </div>
  <div class='input-group'>
    <%= f.label :street %>
    <%= f.text_field :street, class: 'input-group-field' %>
  </div>
  <div class='input-group'>
    <%= f.label :zip %>
    <%= f.text_field :zip, class: 'input-group-field'%>
  </div>
  <div class='input-group'>
    <%= f.label :description %>
    <%= f.text_field :description, class: 'input-group-field' %>
  </div>
    <div class='input-group'>
      <%= f.submit 'Add Microhood', class: 'button' %>
    </div>
  </div>
<% end %>
